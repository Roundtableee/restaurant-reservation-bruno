info:
  name: TC052 - Delete Reservation - Invalid Not Found
  type: http
  seq: 34
  tags:
  - reservation
  - tc052
http:
  method: DELETE
  url: '{{baseUrl}}/reservation/{{fakeReservationId}}'
  auth:
    type: bearer
    token: '{{customerToken}}'
runtime:
  scripts:
  - type: tests
    code: "test(\"HTTP status is 404\", function() {\n  expect(res.status).to.equal(404);\n\
      });\ntest(\"body status is 404\", function() {\n  expect(res.body.status).to.equal(404);\n\
      });\ntest(\"message matches\", function() {\n  expect(res.body.message).to.equal(\"\
      reservation_id not found\");\n});"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
