info:
  name: TC052 - Delete Reservation - Invalid Customer Delete Not Own
  type: http
  seq: 33
  tags:
  - reservation
  - tc052
http:
  method: DELETE
  url: '{{baseUrl}}/reservation/{{walkInDeleteReservationId}}'
  auth:
    type: bearer
    token: '{{otherCustomerToken}}'
runtime:
  scripts:
  - type: tests
    code: "test(\"HTTP status is 403\", function() {\n  expect(res.status).to.equal(403);\n\
      });\ntest(\"body status is 403\", function() {\n  expect(res.body.status).to.equal(403);\n\
      });\ntest(\"message matches\", function() {\n  expect(res.body.message).to.equal(\"\
      forbidden\");\n});"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
