info:
  name: TC049 - Update Reservation - Invalid Logic (Guest/Date/1 Hour Rule)
  type: http
  seq: 30
  tags:
  - reservation
  - tc049
http:
  method: PUT
  url: '{{baseUrl}}/reservation/{{lateRuleReservationId}}'
  body:
    type: json
    data: "{\n  \"Date\": \"{{futureDate2}}\",\n  \"Timeslot\": \"18:30\",\n  \"NumberOfGuests\"\
      : 11,\n  \"SpecialRequest\": \"Birthday setup\",\n  \"TableId\": \"{{smallTableId}}\"\
      \n}"
  auth:
    type: bearer
    token: '{{customerToken}}'
runtime:
  scripts:
  - type: tests
    code: "test(\"HTTP status is 422\", function() {\n  expect(res.status).to.equal(422);\n\
      });\ntest(\"body status is 422\", function() {\n  expect(res.body.status).to.equal(422);\n\
      });\ntest(\"message matches\", function() {\n  expect(res.body.message).to.equal(\"\
      invalid logic\");\n});"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
