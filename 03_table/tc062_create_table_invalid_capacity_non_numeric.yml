info:
  name: TC063 - Create Table - Invalid Capacity Non-Numeric
  type: http
  seq: 9
  tags:
  - table
  - tc063
http:
  method: POST
  url: '{{baseUrl}}/table'
  body:
    type: json
    data: "{\n  \"TableCode\": \"T-07\",\n  \"Capacity\": \"five\"\n}"
  auth:
    type: bearer
    token: '{{adminToken}}'
runtime:
  scripts:
  - type: tests
    code: "test(\"HTTP status is 422\", function() {\n  expect(res.status).to.equal(422);\n\
      });\ntest(\"body status is 422\", function() {\n  expect(res.body.status).to.equal(422);\n\
      });\ntest(\"message matches\", function() {\n  expect(res.body.message).to.equal(\"\
      capacity must be a number\");\n});"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
