info:
  name: TC070 - Update Table - Invalid Logic
  type: http
  seq: 16
  tags:
  - table
  - tc070
http:
  method: PUT
  url: '{{baseUrl}}/table/{{manageTableId}}'
  body:
    type: json
    data: "{\n  \"tableId\": \"{{manageTableId}}\",\n  \"TableCode\": \"\",\n  \"\
      Capacity\": 11,\n  \"status\": \"Occupied\"\n}"
  auth:
    type: bearer
    token: '{{staffToken}}'
runtime:
  scripts:
  - type: tests
    code: "test(\"HTTP status is 422\", function() {\n  expect(res.status).to.equal(422);\n\
      });\ntest(\"body status is 422\", function() {\n  expect(res.body.status).to.equal(422);\n\
      });\ntest(\"message matches\", function() {\n  expect(res.body.message).to.equal(\"\
      invalid logic\");\n});"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
