info:
  name: TC074 - Delete Table - Invalid Table Not Found
  type: http
  seq: 21
  tags:
  - table
  - tc074
http:
  method: DELETE
  url: '{{baseUrl}}/table/{{fakeTableId}}'
  auth:
    type: bearer
    token: '{{adminToken}}'
runtime:
  scripts:
  - type: tests
    code: "test(\"HTTP status is 404\", function() {\n  expect(res.status).to.equal(404);\n\
      });\ntest(\"body status is 404\", function() {\n  expect(res.body.status).to.equal(404);\n\
      });\ntest(\"message matches\", function() {\n  expect(res.body.message).to.equal(\"\
      table not found\");\n});"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
