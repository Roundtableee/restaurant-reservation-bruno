info:
  name: TC009 - Register - Invalid Email Format
  type: http
  seq: 9
  tags:
  - auth
  - tc009
http:
  method: POST
  url: '{{baseUrl}}/auth/register'
  body:
    type: json
    data: "{\n  \"fullName\": \"{{customerFullName}}\",\n  \"password\": \"HelloWorld123\"\
      ,\n  \"email\": \"customer01example.com\",\n  \"phone\": \"{{customerPhone}}\"\
      \n}"
runtime:
  scripts:
  - type: tests
    code: "test(\"HTTP status is 422\", function() {\n  expect(res.status).to.equal(422);\n\
      });\ntest(\"body status is 422\", function() {\n  expect(res.body.status).to.equal(422);\n\
      });\ntest(\"message matches\", function() {\n  expect(res.body.message).to.equal(\"\
      email is invalid\");\n});"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
