info:
  name: TC013 - Change Password - Valid
  type: http
  seq: 14
  tags:
  - auth
  - tc013
http:
  method: POST
  url: '{{baseUrl}}/auth/changepassword'
  body:
    type: json
    data: "{\n  \"newPassword\": \"HelloWorld456\"\n}"
  auth:
    type: bearer
    token: '{{staffToken}}'
runtime:
  scripts:
  - type: tests
    code: "test(\"HTTP status is 200\", function() {\n  expect(res.status).to.equal(200);\n\
      });\ntest(\"body status is 200\", function() {\n  expect(res.body.status).to.equal(200);\n\
      });\ntest(\"message matches\", function() {\n  expect(res.body.message).to.equal(\"\
      change password successful\");\n});"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
