info:
  name: TC015 - Change Password - Invalid Weak Password
  type: http
  seq: 15
  tags:
  - auth
  - tc015
http:
  method: POST
  url: '{{baseUrl}}/auth/changepassword'
  body:
    type: json
    data: "{\n  \"newPassword\": \"123\"\n}"
  auth:
    type: bearer
    token: '{{staffToken}}'
runtime:
  scripts:
  - type: tests
    code: "test(\"HTTP status is 409\", function() {\n  expect(res.status).to.equal(409);\n\
      });\ntest(\"body status is 409\", function() {\n  expect(res.body.status).to.equal(409);\n\
      });\ntest(\"message matches\", function() {\n  expect(res.body.message).to.equal(\"\
      password requirement not met\");\n});"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
