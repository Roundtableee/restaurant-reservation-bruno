info:
  name: TC012 - Login - Invalid Wrong Password
  type: http
  seq: 13
  tags:
  - auth
  - tc012
http:
  method: POST
  url: '{{baseUrl}}/auth/login'
  body:
    type: json
    data: "{\n  \"email\": \"{{customerEmail}}\",\n  \"password\": \"WrongPassword123\"\
      \n}"
runtime:
  scripts:
  - type: tests
    code: "test(\"HTTP status is 401\", function() {\n  expect(res.status).to.equal(401);\n\
      });\ntest(\"body status is 401\", function() {\n  expect(res.body.status).to.equal(401);\n\
      });\ntest(\"message matches\", function() {\n  expect(res.body.message).to.equal(\"\
      wrong email or password\");\n});"
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
